// Filename: app.js
define(['router'], function (Router) {
    return { 
        initialize: function(){
            // Pass in our Router module and call it's initialize function
            Router.initialize();
        }
    };
});
